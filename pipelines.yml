pipelines:
  - name: local
    sources:
      - type: file
        name: demo
        paths:
          - /tmp/log/*.log
        fields:
          topic: "loggie"
    sink:
      type: elasticsearch
      hosts: [ "localhost:9200" ]
      index: "log-${fields.topic}-${+YYYY.MM.DD}"
      username: elastic
      password: Z5IyZxeX1zD8SO6=tP4n
      caCertDir: /tmp/http_ca.crt